# Corelib branches this branch is developed against (docs/CI.md).
# Delete before merging: every entry pins a corelib to unmerged code.
#
# generator#259 / Crucible F-0042: ArrayKind widens to {Unsigned, Signed, Fp32,
# Fp64} and the array-header hook moves past the fixlen_word, so the schema
# `count` bound can be applied only inside the arm matching the declared element
# subtype (CORELIB_PLAN §4.8). It is a two-sided ABI change: generated code is
# the only consumer, so these seven corelibs and this branch must land together.
SOFAB_CORELIB_RS_REF=fix/F-0042-array-count-after-subtype
SOFAB_CORELIB_RS_NO_STD_REF=fix/F-0042-array-count-after-subtype
SOFAB_CORELIB_JAVA_REF=fix/F-0042-array-count-after-subtype
SOFAB_CORELIB_CS_REF=fix/F-0042-array-count-after-subtype
SOFAB_CORELIB_ZIG_REF=fix/F-0042-array-count-after-subtype
SOFAB_CORELIB_DART_REF=fix/F-0042-array-count-after-subtype
SOFAB_CORELIB_GO_REF=fix/F-0042-array-count-after-subtype
