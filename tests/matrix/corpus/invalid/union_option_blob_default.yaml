# A blob option of a union ARRAY ELEMENT with a non-empty default (MESSAGE_SPEC §4.2 / §6).
version: 1
messages: { M: { payload: { v: { id: 0, type: array, items: { type: union, count: 2, oneof: { a: { id: 0, type: u8 }, b: { id: 1, type: blob, maxlen: 4, default: "AQI=" } } } } } } }
