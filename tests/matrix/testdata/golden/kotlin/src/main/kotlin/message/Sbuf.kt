// Code generated by sofabgen; DO NOT EDIT.
@file:OptIn(ExperimentalUnsignedTypes::class)
@file:Suppress("DEPRECATION", "RedundantVisibilityModifier", "unused", "UNUSED_PARAMETER", "RemoveRedundantCallsOfConversionMethods", "KotlinConstantConditions")

package message

import org.sofabuffers.sofab.*

/**
 * Shared support for the generated message classes.
 *
 * `internal`, and not part of any generated type's public surface: a caller
 * uses the message classes, never this.
 */
internal object Sbuf {
    // Shared zero-length defaults: field initializers and reset() reference these
    // instead of allocating a fresh empty array per instance (a decode replaces
    // them anyway, and an empty array has no state to share wrongly).
    internal val EMPTY_UBYTE: UByteArray = UByteArray(0)
    internal val EMPTY_USHORT: UShortArray = UShortArray(0)
    internal val EMPTY_UINT: UIntArray = UIntArray(0)
    internal val EMPTY_ULONG: ULongArray = ULongArray(0)
    internal val EMPTY_BYTE: ByteArray = ByteArray(0)
    internal val EMPTY_SHORT: ShortArray = ShortArray(0)
    internal val EMPTY_INT: IntArray = IntArray(0)
    internal val EMPTY_LONG: LongArray = LongArray(0)
    internal val EMPTY_FLOAT: FloatArray = FloatArray(0)
    internal val EMPTY_DOUBLE: DoubleArray = DoubleArray(0)
    internal val EMPTY_BOOLEAN: BooleanArray = BooleanArray(0)

    /**
     * A growable byte sink.
     *
     * Two jobs, both of them cases where the SIZE is not known in advance:
     * reassembling a string/blob payload the corelib delivered in chunks, and
     * draining the flush sink of an encode whose message the schema cannot
     * bound. [buf] is exposed so a reassembled payload can be converted out of
     * the backing array without a second copy; only `[0, size)` is live.
     */
    internal class Acc {
        internal var buf: ByteArray = ByteArray(64)
            private set
        internal var size: Int = 0
            private set

        internal fun write(data: ByteArray, off: Int, len: Int) {
            if (size + len > buf.size) {
                var n = buf.size * 2
                while (n < size + len) n *= 2
                buf = buf.copyOf(n)
            }
            data.copyInto(buf, size, off, off + len)
            size += len
        }

        internal fun reset() { size = 0 }

        internal fun toByteArray(): ByteArray = buf.copyOf(size)
    }

    /**
     * Enlarge a native array's backing store to hold index [i], doubling but
     * never exceeding [cap] -- the DECLARED element count -- so a valid array
     * ends exactly right-sized.
     *
     * Growth tracks elements actually delivered, so a malformed message
     * claiming ~2^31 elements cannot force an up-front allocation: the wire
     * count is the sender's claim, and only a schema bound is trusted.
     */
    internal fun ensureCapUByte(a: UByteArray, i: Int, cap: Int): UByteArray {
        if (i < a.size) return a
        var n = a.size.toLong() * 2
        if (n < i + 1L) n = i + 1L
        if (n > cap.toLong()) n = cap.toLong()
        return a.copyOf(n.toInt())
    }
    internal fun ensureCapUShort(a: UShortArray, i: Int, cap: Int): UShortArray {
        if (i < a.size) return a
        var n = a.size.toLong() * 2
        if (n < i + 1L) n = i + 1L
        if (n > cap.toLong()) n = cap.toLong()
        return a.copyOf(n.toInt())
    }
    internal fun ensureCapUInt(a: UIntArray, i: Int, cap: Int): UIntArray {
        if (i < a.size) return a
        var n = a.size.toLong() * 2
        if (n < i + 1L) n = i + 1L
        if (n > cap.toLong()) n = cap.toLong()
        return a.copyOf(n.toInt())
    }
    internal fun ensureCapULong(a: ULongArray, i: Int, cap: Int): ULongArray {
        if (i < a.size) return a
        var n = a.size.toLong() * 2
        if (n < i + 1L) n = i + 1L
        if (n > cap.toLong()) n = cap.toLong()
        return a.copyOf(n.toInt())
    }
    internal fun ensureCapByte(a: ByteArray, i: Int, cap: Int): ByteArray {
        if (i < a.size) return a
        var n = a.size.toLong() * 2
        if (n < i + 1L) n = i + 1L
        if (n > cap.toLong()) n = cap.toLong()
        return a.copyOf(n.toInt())
    }
    internal fun ensureCapShort(a: ShortArray, i: Int, cap: Int): ShortArray {
        if (i < a.size) return a
        var n = a.size.toLong() * 2
        if (n < i + 1L) n = i + 1L
        if (n > cap.toLong()) n = cap.toLong()
        return a.copyOf(n.toInt())
    }
    internal fun ensureCapInt(a: IntArray, i: Int, cap: Int): IntArray {
        if (i < a.size) return a
        var n = a.size.toLong() * 2
        if (n < i + 1L) n = i + 1L
        if (n > cap.toLong()) n = cap.toLong()
        return a.copyOf(n.toInt())
    }
    internal fun ensureCapLong(a: LongArray, i: Int, cap: Int): LongArray {
        if (i < a.size) return a
        var n = a.size.toLong() * 2
        if (n < i + 1L) n = i + 1L
        if (n > cap.toLong()) n = cap.toLong()
        return a.copyOf(n.toInt())
    }
    internal fun ensureCapFloat(a: FloatArray, i: Int, cap: Int): FloatArray {
        if (i < a.size) return a
        var n = a.size.toLong() * 2
        if (n < i + 1L) n = i + 1L
        if (n > cap.toLong()) n = cap.toLong()
        return a.copyOf(n.toInt())
    }
    internal fun ensureCapDouble(a: DoubleArray, i: Int, cap: Int): DoubleArray {
        if (i < a.size) return a
        var n = a.size.toLong() * 2
        if (n < i + 1L) n = i + 1L
        if (n > cap.toLong()) n = cap.toLong()
        return a.copyOf(n.toInt())
    }
    internal fun ensureCapBoolean(a: BooleanArray, i: Int, cap: Int): BooleanArray {
        if (i < a.size) return a
        var n = a.size.toLong() * 2
        if (n < i + 1L) n = i + 1L
        if (n > cap.toLong()) n = cap.toLong()
        return a.copyOf(n.toInt())
    }

    /**
     * Store a fresh row of a matrix (an array whose elements are themselves
     * arrays) at the index its element id names, growing the outer list with
     * EMPTY rows so an id GAP decodes as an empty row instead of shifting every
     * later row down by one.
     *
     * Gaps are ordinary: an interior row equal to the element default (the
     * empty row) is omitted by a conformant encoder, and only
     * the LAST row is guaranteed present -- which is what makes the decoded
     * length, highest present id + 1, exact. The row is REPLACED rather than
     * merged into, because an array wrapper IS the array's value: a later
     * occurrence of its field id replaces it whole. The
     * caller's over-index guard bounds the id against the outer array's schema
     * capacity before this grows anything, and [n] is that caller's capped
     * reservation -- never the wire count.
     */
    internal fun placeRowUByte(l: MutableList<UByteArray>, id: Int, n: Int): UByteArray {
        val row = UByteArray(n)
        while (l.size < id) l.add(EMPTY_UBYTE)
        if (l.size == id) l.add(row) else l[id] = row
        return row
    }
    internal fun placeRowUShort(l: MutableList<UShortArray>, id: Int, n: Int): UShortArray {
        val row = UShortArray(n)
        while (l.size < id) l.add(EMPTY_USHORT)
        if (l.size == id) l.add(row) else l[id] = row
        return row
    }
    internal fun placeRowUInt(l: MutableList<UIntArray>, id: Int, n: Int): UIntArray {
        val row = UIntArray(n)
        while (l.size < id) l.add(EMPTY_UINT)
        if (l.size == id) l.add(row) else l[id] = row
        return row
    }
    internal fun placeRowULong(l: MutableList<ULongArray>, id: Int, n: Int): ULongArray {
        val row = ULongArray(n)
        while (l.size < id) l.add(EMPTY_ULONG)
        if (l.size == id) l.add(row) else l[id] = row
        return row
    }
    internal fun placeRowByte(l: MutableList<ByteArray>, id: Int, n: Int): ByteArray {
        val row = ByteArray(n)
        while (l.size < id) l.add(EMPTY_BYTE)
        if (l.size == id) l.add(row) else l[id] = row
        return row
    }
    internal fun placeRowShort(l: MutableList<ShortArray>, id: Int, n: Int): ShortArray {
        val row = ShortArray(n)
        while (l.size < id) l.add(EMPTY_SHORT)
        if (l.size == id) l.add(row) else l[id] = row
        return row
    }
    internal fun placeRowInt(l: MutableList<IntArray>, id: Int, n: Int): IntArray {
        val row = IntArray(n)
        while (l.size < id) l.add(EMPTY_INT)
        if (l.size == id) l.add(row) else l[id] = row
        return row
    }
    internal fun placeRowLong(l: MutableList<LongArray>, id: Int, n: Int): LongArray {
        val row = LongArray(n)
        while (l.size < id) l.add(EMPTY_LONG)
        if (l.size == id) l.add(row) else l[id] = row
        return row
    }
    internal fun placeRowFloat(l: MutableList<FloatArray>, id: Int, n: Int): FloatArray {
        val row = FloatArray(n)
        while (l.size < id) l.add(EMPTY_FLOAT)
        if (l.size == id) l.add(row) else l[id] = row
        return row
    }
    internal fun placeRowDouble(l: MutableList<DoubleArray>, id: Int, n: Int): DoubleArray {
        val row = DoubleArray(n)
        while (l.size < id) l.add(EMPTY_DOUBLE)
        if (l.size == id) l.add(row) else l[id] = row
        return row
    }
    internal fun placeRowBoolean(l: MutableList<BooleanArray>, id: Int, n: Int): BooleanArray {
        val row = BooleanArray(n)
        while (l.size < id) l.add(EMPTY_BOOLEAN)
        if (l.size == id) l.add(row) else l[id] = row
        return row
    }

    /**
     * [placeRow] for a row that is itself a wrapper array (a list): same
     * id-keyed placement and the same gap fill with the empty row.
     *
     * "Replaced" is a statement about the VALUE, not the object: an
     * already-present row is emptied in place rather than swapped for a fresh
     * list, so decoding N rows allocates N lists and not 2N.
     */
    internal fun <T> placeRowList(l: MutableList<MutableList<T>>, id: Int) {
        while (l.size < id) l.add(mutableListOf())
        if (l.size == id) { l.add(mutableListOf()); return }
        l[id].clear()
    }

    /**
     * A boolean array as the `0`/`1` unsigned array it is on the wire
     * as an unsigned array. The one native element kind with no OStream overload
     * of its own, so it is the one that has to be materialised for the write.
     */
    internal fun boolBytes(v: BooleanArray): ByteArray {
        val a = ByteArray(v.size)
        for (i in v.indices) a[i] = if (v[i]) 1 else 0
        return a
    }
}
